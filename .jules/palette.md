## 2024-05-24 - [Actionable CLI Errors]
**Learning:** CLI error messages that simply state "Invalid value" leave users guessing, whereas providing the list of valid options directly in the error message significantly reduces friction and improves the developer experience.
**Action:** Always include the allowed values/formats in validation error messages for CLI inputs to make them immediately actionable.
